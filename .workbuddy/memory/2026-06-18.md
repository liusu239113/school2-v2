
## 从 GitHub 拉取最新代码覆盖本地
- 仓库: liusu239113/school2.git
- 更新了 remote school2 的 token
- git fetch + git reset --hard 到 school2/main
- 最新提交: d515881 fix: 修复编译错误——移除对 CustomController 属性getter的错误重写

## 修复玩家反馈问题 + 推送GitHub触发打包
- 新增广告源lib（maimeng, tosin-ad升级等）已包含在本次提交中
- **版本号更新**: versionCode 23, versionName "3.1"
- **Bug修复1**: 存读档数据丢失（毕业生数据清零/数据回退）
  - 新增 GameEngine.flushAllManagerStates() 方法，存档前强制将全部 Manager 内存状态刷入 DB
  - SaveViewModel.performSave 在拷贝 DB 文件前先刷入最新状态
- **Bug修复2**: 考试成绩不自动更新
  - GameEngine 新增 gameDaySignal (SharedFlow)，每次 tick 发射
  - ExamViewModel 监听 gameDaySignal 自动刷新考试成绩界面
- **功能新增**: 校长办公室奢侈品统计
  - Principal 新增 purchasedLuxuryItems 列表字段（通过 principalJson 自动持久化）
  - PrincipalOfficeScreen 新增"已购奢侈品"汇总卡片（件数、总消费、分类统计）
  - 已购物品在列表中直接显示"已购买 ✓"
- 推送 school2/main 成功，触发远端打包
